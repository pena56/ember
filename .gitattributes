# Normalize line endings: text files are stored as LF in the repo and checked
# out as LF on every OS (incl. Windows), so files don't perpetually show as
# "modified" due to CRLF churn (which also tripped the doc-sync Stop hook).
# Git auto-detects binary files and leaves them untouched.
* text=auto eol=lf
