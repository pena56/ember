# Expo export / prebuild output
dist/

# Generated by scripts/bundle-pdfjs.mjs (predev/typecheck) — pdf.js inlined for the WebView reader
src/reader/pdf-js-content.ts

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Firebase Cloud Messaging V1 service-account private key — SECRET, never commit.
# Upload it to EAS credentials instead (eas credentials → Android → FCM V1).
# (google-services.json is NOT secret and is intentionally committed so EAS builds can read it.)
*-service-account*.json
firebase-adminsdk*.json